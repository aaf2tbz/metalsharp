/// @file PipelineState.cpp
/// @brief Metal render pipeline state C++ interface — portable pipeline logic.
///
/// C++ portion of the MTLRenderPipelineState wrapper. Declares the pipeline state
/// descriptor and cache interface used by the D3D translation layer.

#include <metalsharp/PipelineState.h>

namespace metalsharp {
}
