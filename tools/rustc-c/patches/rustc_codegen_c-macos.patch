diff --git a/src/intrinsic.rs b/src/intrinsic.rs
index 235b903..ee7d70d 100644
--- a/src/intrinsic.rs
+++ b/src/intrinsic.rs
@@ -28,6 +28,19 @@ impl<'a, 'tcx> IntrinsicCallBuilderMethods<'tcx> for Builder<'a, 'tcx> {
         let name = self.cx.tcx.item_name(instance.def_id());
 
         match name {
+            sym::ptr_mask => {
+                // Match llvm.ptrmask using the same integer round-trip as
+                // rustc_codegen_gcc.
+                let ptr = args[0].immediate();
+                let mask = args[1].immediate();
+                let usize_ty = self.cx.type_isize();
+                let result_ty = self.cx.backend_type(result_dest.layout);
+                let addr = self.ptrtoint(ptr, usize_ty);
+                let masked = self.and(addr, mask);
+                let result = self.inttoptr(masked, result_ty);
+                self.store(result, result_dest.val.llval, result_dest.val.align);
+                Ok(())
+            }
             sym::black_box => {
                 args[0].val.store(self, result_dest);
                 // Use C11 atomic_signal_fence as a portable optimization
diff --git a/src/write.rs b/src/write.rs
index c008d93..e0de007 100644
--- a/src/write.rs
+++ b/src/write.rs
@@ -60,6 +60,14 @@ pub(crate) fn codegen(
     // recursion to loops, preventing stack overflow
     // detection (e.g. tests/ui/runtime/out-of-stack.rs)
 
+    // Keep generated C objects on Rust's macOS availability floor.  Direct
+    // clang invocation does not otherwise inherit this setting.
+    if let Ok(target) = std::env::var("MACOSX_DEPLOYMENT_TARGET") {
+        if !target.trim().is_empty() {
+            cmd.arg(format!("-mmacosx-version-min={target}"));
+        }
+    }
+
     // -fPIC: skip for MSVC-like compilers where it's not applicable
     let cc_lower = cc.to_lowercase();
     if !cc_lower.contains("cl.exe") && !cc_lower.ends_with("\\cl") {
