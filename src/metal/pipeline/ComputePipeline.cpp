/// @file ComputePipeline.cpp
/// @brief Metal compute pipeline C++ interface — portable compute pipeline logic.
///
/// C++ portion of the MTLComputePipelineState wrapper. Declares the compute pipeline
/// interface for dispatch setup used by the D3D translation layer.

#include <metalsharp/MetalBackend.h>

namespace metalsharp {}
