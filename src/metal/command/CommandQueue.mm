/// @file CommandQueue.mm
/// @brief Metal command queue — MTLCommandQueue wrapper for command buffer scheduling.
///
/// Objective-C++ implementation wrapping MTLCommandQueue. Provides command buffer
/// allocation and submission to the Metal GPU scheduler.

#include <metalsharp/MetalBackend.h>

namespace metalsharp {

}
