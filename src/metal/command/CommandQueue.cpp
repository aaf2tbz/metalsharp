/// @file CommandQueue.cpp
/// @brief Metal command queue C++ interface — portable queue management logic.
///
/// C++ portion of the MTLCommandQueue wrapper. Declares the command queue
/// interface that .mm files implement against the Objective-C Metal API.

#include <metalsharp/MetalBackend.h>

namespace metalsharp {}
